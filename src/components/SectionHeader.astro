---
interface Props {
  id?: string;
  /** Heading level. Sections are h2; the first heading on a subpage is h1. */
  as?: "h1" | "h2";
}

const { id, as: Tag = "h2" }: Props = Astro.props;
---

<Tag id={id} class="section-head">
  <slot />
</Tag>
